<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name="configure.test.configurebuilds.StartUp"
        android:label="@string/app_name">
        <activity
            android:name="configure.test.configurebuilds.ui.DataBindingListActivity"
            android:label="@string/title_activity_data_binding_list"
            android:parentActivityName="configure.test.configurebuilds.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="configure.test.configurebuilds.MainActivity" />
        </activity>
        <activity
            android:name="configure.test.configurebuilds.ui.activities.test101.DataBinding101Activity"
            android:label="@string/title_activity_data_binding101"
            android:parentActivityName="configure.test.configurebuilds.ui.DataBindingListActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="configure.test.configurebuilds.ui.DataBindingListActivity" />
        </activity>
        <activity
            android:name="configure.test.configurebuilds.ui.activities.test102.Expression102Activity"
            android:label="@string/title_activity_expression102"
            android:parentActivityName="configure.test.configurebuilds.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="configure.test.configurebuilds.MainActivity" />
        </activity>
        <activity
            android:name="configure.test.configurebuilds.ui.activities.test103.EventHandlerActivity"
            android:parentActivityName="configure.test.configurebuilds.ui.DataBindingListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="configure.test.configurebuilds.ui.DataBindingListActivity" />
        </activity>
    </application>

</manifest>